<h1><%= @wiki.name %> collaborators</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>User</th>
    </tr>
  </thead>

  <tbody>
    <% @collaborators.each do |collaborator| %>
      <tr>
        <td><%= collaborator.user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Collaborator', new_wiki_collaborator_path %> |
<%= link_to 'Back', wikis_path %>
